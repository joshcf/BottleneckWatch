# BottleneckWatch Dependencies
psutil>=5.9.0          # Cross-platform system monitoring
pystray>=0.19.0        # System tray functionality
Pillow>=10.0.0         # Icon generation (capital P for proper package name)
WMI>=1.5.1             # Windows-specific metrics
pywin32>=306           # Windows API access
matplotlib>=3.8.0      # Graphing
